DEPENDENCY REVIEW - QUICK START GUIDE
======================================

COPY-PASTE COMMANDS FOR IMMEDIATE ACTIONS
============================================

Step 1: Create Lock Files (5 minutes)
─────────────────────────────────────
cd /home/user/sc-generator

# Create NPM lock file
npm ci --package-lock-only

# Create Python lock file  
pip freeze > requirements.lock

# Commit to git
git add package-lock.json requirements.lock
git commit -m "chore: add dependency lock files for reproducible builds"


Step 2: Update axios (10 minutes)
──────────────────────────────────
npm install axios@1.18.1

# Test API calls work
npm test -- --testNamePattern="api|fetch|axios"

# Or full test suite
npm test


Step 3: Update React (10 minutes)
──────────────────────────────────
npm install react@18.3.1 react-dom@18.3.1

# Run tests
npm test

# Manual check
npm start  # Browse app, check console for warnings


Step 4: Commit Changes
──────────────────────
git add package.json package-lock.json
git commit -m "chore: upgrade react 18.2.0→18.3.1 and axios 1.6.0→1.18.1

- axios: update to 1.18.1 for security fixes and HTTP improvements
- react/react-dom: patch update for bug fixes
- All tests passing, no breaking changes"


Step 5: Plan Flask 3 Migration (Review only, don't execute yet)
────────────────────────────────────────────────────────────────
# This is HIGH RISK - needs proper testing in a feature branch

git checkout -b upgrade/flask3

# Update requirements.txt
# Change:
#   Flask==2.3.3
#   Werkzeug==2.3.7
# To:
#   Flask==3.0.3
#   Werkzeug==3.0.3

pip install Flask==3.0.3 Werkzeug==3.0.3

# Run tests
pytest

# Manual test - start app and verify
python app.py
# Test encryption endpoints manually

# If all works:
git commit -m "chore: upgrade Flask 2.3.3→3.0.3 and Werkzeug 2.3.7→3.0.3

BREAKING CHANGES CHECKED:
- JSON encoding updated
- Blueprint registration verified
- Session handling compatible
- CORS still functional

All endpoints tested and working."

# Then merge:
git checkout main
git merge upgrade/flask3


═══════════════════════════════════════════════════════════════════════════

VERIFICATION CHECKLIST
══════════════════════

After each step, verify:

□ No TypeScript/JSX errors
□ No console warnings
□ npm audit shows no HIGH severity issues
□ All existing tests pass
□ App starts without errors
□ API endpoints respond correctly

For Flask: 
□ pytest all passing
□ No deprecation warnings
□ API endpoints tested manually
□ CORS requests working


WHAT NOT TO DO
═══════════════

✗ Do NOT update React to 19.x yet
✗ Do NOT update Flask-CORS without testing Flask 3 first
✗ Do NOT commit lock files without reviewing them first
✗ Do NOT skip the pytest tests when updating Flask
✗ Do NOT merge Flask 3 branch without manual testing


TIMELINE SUMMARY
═════════════════

THIS WEEK:
  Mon: Steps 1-4 (NPM updates + lock files)
  
NEXT WEEK:
  Mon-Wed: Step 5 (Flask 3 migration in feature branch)
  Thu-Fri: Testing, fixes, merge

Total effort: ~4-6 hours


QUESTIONS? CHECK THESE FILES
═════════════════════════════

Full analysis:
  /tmp/claude-0/.../scratchpad/dependency_analysis.txt

Version details:
  /tmp/claude-0/.../scratchpad/version_comparison.txt

Full recommendations:
  /tmp/claude-0/.../scratchpad/RECOMMENDATIONS.txt

